<h1>Listing action_items</h1>

<table>
  <tr>
    <th>Author</th>
    <th>Approved</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @action_items.each do |action_item| %>
  <tr>
    <td><%= action_item.author_id %></td>
    <td><%= action_item.approved %></td>
    <td><%= action_item.body %></td>
    <td><%= link_to 'Show', action_item %></td>
    <td><%= link_to 'Edit', edit_action_item_path(action_item) %></td>
    <td><%= link_to 'Destroy', action_item, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Action item', new_action_item_path %>
